# macMLX v0.1 — Master Implementation Overview

> **For agentic workers:** This file is the contract for what gets shipped in v0.1 and the order of work. Each Stage has (or will have) its own detailed task-by-task plan in this directory. Use `superpowers:subagent-driven-development` to execute Stage plans.

**Goal:** Ship the first working version of macMLX — a native macOS LLM inference app with SwiftUI GUI, CLI + TUI, and OpenAI-compatible HTTP API, distributed via DMG with Sparkle auto-update.

**Architecture:** Three-target Swift monorepo. `MacMLXCore` SPM package owns all inference, model management, settings, and HTTP server logic. `macMLX.app` (Xcode project) and `macmlx` (SPM executable) are thin shells that import Core. Engine implementations sit behind a single `InferenceEngine` Swift protocol so the rest of the app is engine-agnostic.

**Tech Stack:** Swift 6.3 strict concurrency · SwiftUI (macOS 14+) · `mlx-swift-lm` (Apple SPM) · `Hummingbird` (HTTP server) · `swift-argument-parser` · `SwiftTUI` · `Sparkle 2.x` · Swift Testing.

---

## Source-of-Truth Spec Files

All design decisions are in `.claude/`. Plans MUST reference these — never invent behavior:

| Concern | File |
|---|---|
| Module layout, package structure, protocols | `.claude/architecture.md` |
| Swift conventions (concurrency, naming, errors) | `.claude/swift-conventions.md` |
| UI design language (colors, fonts, layout) | `.claude/ui-guidelines.md` |
| HTTP API contracts | `.claude/api-contracts.md` |
| Sparkle, DMG, GitHub Actions | `.claude/distribution.md` |
| Per-feature behavior | `.claude/features/*.md` |

---

## Stage Breakdown

### Stage 0 — Pre-flight (DONE in this session)

Environment verification, README correction, plan authoring.

- ✅ Xcode 26.4 + Swift 6.3 + macOS SDK 26.4 active
- ✅ Git remote `magicnight/Mac-MLX.git` reachable
- ✅ `mlx-swift-lm` GitHub repo reachable
- ✅ README v0.1 checkboxes reset to `[ ]`
- ✅ This overview + Stage 1 plan written

### Stage 1 — Bootstrap

**Plan:** `v0.1-stage1-bootstrap.md` (this directory)

**Output:** Three compilable empty targets + smoke tests + CI workflow.

- `MacMLXCore/Package.swift` with `mlx-swift-lm` and `Hummingbird` dependencies
- `macmlx-cli/Package.swift` depending on local `MacMLXCore`, `swift-argument-parser`, `SwiftTUI`
- `macMLX/macMLX.xcodeproj` — empty SwiftUI app, embeds `MacMLXCore` via local SPM, `LSUIElement=YES`
- `.github/workflows/ci.yml` runs `swift test` for both SPM packages
- All three build green

**Exit criteria:** `swift test --package-path MacMLXCore` and `swift test --package-path macmlx-cli` both pass; `xcodebuild -scheme macMLX build` succeeds; CI green on push.

**Status:** ✅ Done (2026-04-16). Commits `2efdff0..684a4b7`. CI green at https://github.com/magicnight/Mac-MLX/actions/runs/24522258021 (Xcode App Build 48s · SPM Build & Test 1m50s).

### Stage 2 — Core Protocols + Domain Types

**Plan:** `v0.1-stage2-core-types.md` (write at start of Stage 2)

**Output:** Type-only foundation for the engine layer, fully unit-tested.

- `InferenceEngine` protocol (Actor)
- `EngineID`, `EngineStatus`, `LocalModel`, `HFModel`, `BenchmarkResult`
- `GenerateRequest`, `GenerateChunk`
- `EngineError`, `InferenceServiceError` typed errors
- `MemoryProbe` (`hw.memsize` reader)

**Exit criteria:** Every type has at least one Swift Testing test; `swift test` green; protocol conformances stub-able for downstream module tests.

**Status:** ✅ Done (2026-04-16). Commits `b5bc7c7..fdbebd2` (6 commits, ~470 LOC source + tests). 29/29 Swift Testing tests passing. CI green: https://github.com/magicnight/Mac-MLX/actions/runs/24523081407 (Xcode App Build 55s · SPM Build & Test 2m3s).

### Stage 3 — Core Modules (Parallel via Worktrees)

**Plans:** four sub-plans, one per worktree, written at start of Stage 3.

| Worktree | Module | Spec |
|---|---|---|
| `wt/engine` | `MLXSwiftEngine` | `features/inference-engines.md` §Engine 1 |
| `wt/library` | `ModelLibraryManager` + `HFDownloader` | `features/model-downloader.md` |
| `wt/settings` | `SettingsManager` + `LogManager` | `architecture.md` §Settings Schema, `features/logs.md` |
| `wt/server` | `HummingbirdServer` (OpenAI-compatible) | `api-contracts.md` |

Use `superpowers:using-git-worktrees` to create isolation, then `superpowers:dispatching-parallel-agents` to run 4 streams concurrently.

**Exit criteria:** All 4 worktrees merged into `main`; integration test loads `mlx-community/Qwen3-1.7B-4bit`, generates a token, returns via `/v1/chat/completions` SSE.

### Stage 4 — SwiftUI Surfaces (Sequential)

**Plans:** five sub-plans, one per surface, written at start of Stage 4.

In order:

1. `MenuBarManager` + popover (`features/menubar.md`)
2. Onboarding 5-step window (`features/onboarding.md`)
3. Model Library view (`features/model-downloader.md`)
4. Chat view with streaming (`features/chat-ui.md`)
5. Settings view + engine picker (`features/inference-engines.md` §Engine Selection UI)

Each surface lands with `#Preview` variants matching `ui-guidelines.md`. Visual review checkpoint by user before next surface starts.

**Exit criteria:** `macMLX.app` launches, onboarding completes on fresh install, user can download a model, load it, send a chat message, and see streaming response. Menu bar shows live status.

### Stage 5 — CLI + TUI

**Plan:** `v0.1-stage5-cli-tui.md` (write at start of Stage 5)

**Output:** `macmlx` binary with `serve`, `pull`, `run`, `list`, `ps`, `stop`. TUI dashboards for `serve`, `pull`, `run` (via SwiftTUI). `--json` flag for `list` and `ps`.

**Exit criteria:** `macmlx run Qwen3-1.7B-4bit "hello"` prints a response. `macmlx serve` starts the server with TUI dashboard.

### Stage 6 — Distribution

**Plan:** `v0.1-stage6-distribution.md` (write at start of Stage 6)

**Output:** Tagged release `v0.1.0` with signed DMG and working appcast.

- Sparkle EdDSA keypair generated (private key in `SPARKLE_PRIVATE_KEY` GitHub Secret)
- `appcast.xml` template with placeholder entries
- `.github/workflows/release.yml` builds, packages, signs, publishes
- `scripts/package-dmg.sh` and `scripts/update_appcast.py`
- First DMG built locally and tested end-to-end (download → install → first launch → load model)

**Exit criteria:** `git tag v0.1.0 && git push --tags` produces a public GitHub Release with a downloadable DMG that installs and runs on a clean Mac.

---

## Dependency Graph

```
Stage 0 (done)
   │
   ▼
Stage 1 (bootstrap) ───────┐
   │                       │
   ▼                       │
Stage 2 (core types) ──────┘
   │
   ▼
Stage 3 (core modules) ── 4 parallel worktrees
   │
   ▼
Stage 4 (SwiftUI surfaces, sequential) ──┐
   │                                     │
   └───── independent ──── Stage 5 (CLI/TUI)
                          │
                          ▼
                       Stage 6 (distribution)
```

Stage 4 and Stage 5 can run in parallel after Stage 3 (different humans/agents). Stage 6 starts when both Stage 4 and Stage 5 reach their exit criteria.

---

## Out of Scope for v0.1

Per `.claude/CLAUDE.md` directive ("Only implement items marked `v0.1` in each feature file"):

- SwiftLM engine subprocess wiring (detection only)
- Python MLX engine subprocess wiring (detection only)
- VLM support
- Conversation persistence
- Benchmark feature (entire `features/benchmark.md`)
- Logs viewer UI (`features/logs.md` Pulse integration is Stage 6 stub only)
- Homebrew tap (v0.2)
- Inspector panel, message editing, parameter UI beyond defaults

If any of these come up during execution, drop a `// TODO: v0.2` comment and skip.

---

## Stage Completion Protocol

After every Stage:

1. All exit criteria verified (run the actual commands, paste output)
2. README v0.1 checkbox flipped to `[x]` only when the corresponding feature actually works end-to-end
3. CHANGELOG `[Unreleased]` updated
4. Commit on `main` with message `feat(stage-N): <summary>`
5. This overview file updated to mark Stage as done
