// SettingsView.swift
// macMLX
//
// STUB — fleshed out in Stage 4 Task 7.

import SwiftUI

struct SettingsView: View {
    var body: some View {
        ContentUnavailableView(
            "Settings",
            systemImage: "gear",
            description: Text("Engine picker and server config arrive in Stage 4 Task 7.")
        )
    }
}

#Preview {
    SettingsView()
}
