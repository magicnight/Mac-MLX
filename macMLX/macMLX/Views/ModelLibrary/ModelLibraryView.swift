// ModelLibraryView.swift
// macMLX
//
// STUB — fleshed out in Stage 4 Task 5.

import SwiftUI

struct ModelLibraryView: View {
    var body: some View {
        ContentUnavailableView(
            "Model Library",
            systemImage: "tray.full",
            description: Text("Local + Hugging Face model browser arrives in Stage 4 Task 5.")
        )
    }
}

#Preview {
    ModelLibraryView()
}
