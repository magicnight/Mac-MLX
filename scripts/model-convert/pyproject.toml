[project]
name = "macmlx-model-convert"
version = "0.1.0"
description = "Local dev tool — re-quantize MLX models (mxfp4/mxfp8 → affine) so mlx-swift-lm can load them. Not shipped with Mac-MLX."
requires-python = ">=3.13"
dependencies = [
    "mlx-lm>=0.22",
]

[tool.uv]
package = false
