function faq(id, factIds, enQuestion, enAnswer, zhQuestion, zhAnswer) {
  return Object.freeze({ id, factIds: Object.freeze(factIds), en: Object.freeze({ question: enQuestion, answer: enAnswer }), "zh-Hans": Object.freeze({ question: zhQuestion, answer: zhAnswer }) });
}

export const faqs = Object.freeze([
  faq("platform-installation", ["platform-installation"], "What hardware does macMLX support, and what if Gatekeeper blocks it?", "macMLX supports Apple Silicon Macs running macOS 14 or later. Follow the project's current installation and Gatekeeper guidance for the unsigned app; do not disable system-wide security protections solely to open macMLX.", "macMLX 支持哪些硬件，Gatekeeper 阻止时怎么办？", "macMLX 支持运行 macOS 14 或更高版本的 Apple 芯片 Mac。未签名应用请遵循项目当前安装与 Gatekeeper 指南；不要仅为打开 macMLX 而关闭系统级安全保护。"),
  faq("python", ["no-python-default"], "Does macMLX require Python?", "Not for the default released engine, app, or CLI inference path. Optional compatibility engines may use other runtimes, so macMLX does not claim that Python is absent everywhere.", "macMLX 需要 Python 吗？", "已发布的默认引擎、应用与 CLI 推理路径不需要 Python。可选兼容引擎可能使用其他运行时，因此 macMLX 不宣称任何地方都没有 Python。"),
  faq("model-selection", ["unified-memory", "model-pool", "lora", "vlm-14-families"], "Which model format and size should I choose?", "Choose an MLX checkpoint whose exact architecture is supported. For capacity, subtract macOS and app headroom from physical unified memory, then budget weights, KV cache, activations, and any concurrent models; if the total approaches the remainder, choose a smaller or more strongly quantized checkpoint.", "应该选择哪种模型格式与规模？", "请选择确切架构受支持的 MLX 检查点。容量规划时，先从物理统一内存中扣除 macOS 与应用余量，再估算权重、KV 缓存、激活值和并发模型；若总量接近剩余容量，应选择更小或量化程度更高的检查点。"),
  faq("apis", ["openai-compat", "anthropic-messages", "ollama-selected"], "Which APIs are compatible?", "v0.5.3 supports specific OpenAI endpoint families, Anthropic Messages, and five selected Ollama endpoints. The API matrix lists the boundaries; provider-wide drop-in compatibility is not claimed.", "兼容哪些 API？", "v0.5.3 支持特定 OpenAI 端点家族、Anthropic Messages 与五个选定 Ollama 端点。API 矩阵会列出边界，不宣称对任何提供方实现完整无差别兼容。"),
  faq("privacy", ["swift-in-process"], "Does inference stay local?", "The default inference engine and HTTP server run locally on the Mac. Network access is still used when you download models, query remote registries, or configure external tools.", "推理会留在本地吗？", "默认推理引擎与 HTTP 服务在 Mac 本地运行。下载模型、查询远程仓库或配置外部工具时仍会使用网络。"),
  faq("vlm", ["vlm-14-families"], "Can macMLX run vision-language models?", "Yes. The v0.5.3 model library detects 14 VLM model_type families. Exact checkpoint, processor, quantization, and memory compatibility still need to be checked.", "macMLX 可以运行视觉语言模型吗？", "可以。v0.5.3 模型库可检测 14 个 VLM model_type 家族；仍需核对具体检查点、处理器、量化与内存兼容性。"),
  faq("large-moe", ["deepseek-v32", "model-pool"], "What about very large MoE models?", "v0.5.3 includes a DeepSeek V3.2 Swift overlay with component parity, but real-checkpoint smoke testing and FP8 dequantization remain gaps. Optional engines can serve different compatibility needs.", "超大 MoE 模型怎么办？", "v0.5.3 包含具有组件对齐的 DeepSeek V3.2 Swift 覆盖层，但真实检查点冒烟测试与 FP8 反量化仍有缺口。可选引擎可覆盖不同兼容需求。"),
  faq("roadmap", ["continuous-batching", "trie-lcp", "paged-kv", "adaptive-memory-guard", "sampling-expanded"], "How do released and roadmap features differ?", "v0.5.3 is the release baseline. Limited continuous batching and longest-prefix reuse are post-tag development; paged KV, block sharing, CoW, a unified adaptive guard, and expanded sampling controls are planned.", "已发布功能与路线图如何区分？", "v0.5.3 是发布基线。受限连续批处理与最长前缀复用属于标签后的开发工作；分页 KV、块共享、CoW、统一自适应守卫与扩展采样控制处于规划阶段。"),
]);
